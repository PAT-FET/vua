export interface SkeletonAvatar {
    size: 'sm' | 'md' | 'lg'
    shape: 'circle' | 'square'
}

export interface SkeletonParagraph {
    width: string | string[]
    rows: number
}

export interface SkeletonTitle {
    width: string
}
